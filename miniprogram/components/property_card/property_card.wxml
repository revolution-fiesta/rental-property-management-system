<!--components/property-card/property_card.wxml-->
<view class="card">
  <image class="card-image" src="{{imageSrc}}" mode="aspectFill"></image>
  <view class="card-content">
    <view class="card-header">
      <view class="card-price">{{price}}</view>
      <view class="card-actions">
        <view class="action-btn">{{action1}}</view>
        <view class="action-btn">{{action2}}</view>
      </view>
    </view>
    <view class="card-title">{{title}}</view>
    <view class="card-info">{{info}}</view>
  </view>
</view>